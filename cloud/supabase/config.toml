# Minimal Supabase CLI config. The live project ref is passed via --project-ref at deploy
# time (and kept in cloud/.secrets.local), so nothing environment-specific lives here.
project_id = "agentbreaker"

[functions.ingest]
verify_jwt = false

[functions.control]
verify_jwt = false
